import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sayvabr.jogoduo',
  appName: 'Jogo Duo',
  webDir: 'dist',
  android: { backgroundColor: '#101426', allowMixedContent: false }
};
export default config;
